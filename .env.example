# 이 파일을 .env 로 복사한 뒤 값을 채우세요.
# .env 는 커밋되지 않습니다 (.gitignore).

# LLM 호출용 API 키 (필수)
ANTHROPIC_API_KEY=

# 이슈 조회·생성용 personal access token (필수)
# public 저장소만 다루면 public_repo 스코프면 충분합니다.
GITHUB_TOKEN=

# 커밋 메시지 생성에 쓸 모델 (선택, 기본값 claude-sonnet-5)
COMMIT_AGENT_MODEL=
